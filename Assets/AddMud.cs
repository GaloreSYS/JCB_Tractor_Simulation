using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AddMud : MonoBehaviour
{
    private void Start()
    {
        Destroy(this,2f);
    }

    private void Update()
    {
      //  FF_Digger.Instance.AddTerrain(transform,0.2f);    
    }
}
